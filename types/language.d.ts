export type Lang = {
  title: {
    search: string
    menu: {
      file: string
      edit: string
      selection: string
      view: string
      go: string
      terminal: string
      help: string
    }
  }
}
